spring:
  datasource:
    url: jdbc:h2:mem:bookdb
  shell:
    interactive:
      enabled: false
  h2:
    console:
      enabled: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master-test.yaml